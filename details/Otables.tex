\hspace*{-.25in}
\begin{tikzpicture}[thick,scale=1, every node/.style={scale=1.3}]

      \path[use as bounding box] (-2.75,3.5) rectangle (10,-2);

      \path[->]<1-> node[proctest, minimum width=7ex]
        (doubler) at (0,1.25) {
              \begin{tabular}{l}
         Effectiveness \\ Evaluation
    \end{tabular}
        };

      \path[->]<2-> node[proc, right of=doubler, yshift=0.54in,
      xshift=1.25in,minimum width=21ex]
      (exps) {
      \begin{tabular}{l}
         Generate test data \\ for each case study
    \end{tabular}

           };

    \path[->]<3-> node[proc, below of=exps, yshift=-0.15in,
      xshift=0in,minimum width=21ex]
      (first) {
            \begin{tabular}{l}
         Sample 20 queries \\ for each case study
        \end{tabular}
        };

    \path[->]<4-> node[proc, below of=first, yshift=-0.04in,
      xshift=0in,minimum width=21ex]
      (seccond) {Manually verify accuracy};

    \path[->]<5-> node[special, below of=seccond, yshift=0.05in,
      xshift=0in,minimum width=21ex]
      (third) {Report percent accuracy};

    % \path[->]<6-> node[special, below of=third, yshift=-0.10in,
    %   xshift=-0.75in,minimum width=21ex]
    %   (takeaway) {\textit{SchemaAnalyst} $\in O(n)$ for constraints studied};

\end{tikzpicture}
